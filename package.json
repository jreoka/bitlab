{
  "name": "bitlab-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Bitlab Stremio addon as a Cloudflare Worker",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
